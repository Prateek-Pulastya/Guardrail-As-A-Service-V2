# GuardRail-as-a-Service — Python dependencies
# -----------------------------------------------
# Tested on Python 3.11, 3.12, 3.13, 3.14 (Windows/Mac/Linux)
# Last updated: June 2026
#
# Version notes:
#   onnxruntime==1.18.* has NO wheel for Python 3.12+.
#   Use 1.26.* which ships cp312/cp313/cp314 wheels.
#   optimum 1.20.* requires onnxruntime<1.22; bumped to 1.26.* to match.
#   transformers 4.41.* is compatible with optimum 1.26.*.
#   numpy pinned to 1.26.* for onnxruntime ABI compatibility.
#   torch (~2GB) is an unavoidable transitive dep of optimum; it is only
#   used during model download/quantization, not during inference.

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.115.*
uvicorn[standard]==0.30.*
pydantic==2.7.*

# ── Tier 1: pattern matching ───────────────────────────────────────────────
pyahocorasick==2.1.*
pyyaml==6.0.*

# ── Tier 2: ML classifier ─────────────────────────────────────────────────
transformers==4.41.*
optimum[onnxruntime]==1.26.*
onnxruntime==1.26.*
numpy==1.26.*

# ── Observability ──────────────────────────────────────────────────────────
prometheus-client==0.20.*

# ── Evaluation harness ────────────────────────────────────────────────────
httpx==0.27.*
rich==13.*
scipy==1.13.*
pandas==2.2.*

# ── Dev / test ─────────────────────────────────────────────────────────────
pytest==8.*
pytest-asyncio==0.23.*