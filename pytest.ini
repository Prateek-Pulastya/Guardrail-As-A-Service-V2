[pytest]
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    unit: marks unit tests (no service required)
    integration: marks integration tests (service required)
testpaths = tests
